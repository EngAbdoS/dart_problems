double formula(double p, double t, double r) {
  return (p * t * r) / 100;
}

void main() {
//Write a program in Dart that finds simple interest. Formula= (p * t * r) / 100
  print(formula(5, 5, 5));
}
